
todo:

LibraryInfo:
    добавить name;
    +name (linux);
    +binpath (windows);

может быть:
    cmd (windows)
    для удобства сделать статичный указатель на mystruct;
    использовать windows-link вместо windows-sys